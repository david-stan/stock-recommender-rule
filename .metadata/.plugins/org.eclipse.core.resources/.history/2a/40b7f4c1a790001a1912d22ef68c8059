package sbnz.integracija;

import sbnz.integracija.example.facts.Item;


rule "Classify Item - Low Range"
	no-loop
    when
        $i: Item( cost <= 400 && cost >= 200, $c: cost )
    then
        $i.setCategory(Item.Category.LOW_RANGE);
        $i.setCost($c * 0.9);
        update($i);
end